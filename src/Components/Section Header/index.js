import React from 'react'

export const SectionHeader = ({name}) => (
    <div className="content-container section-header">
        <div className="content d-flex justify-content-center">
            <h2>{name}</h2>
        </div>
    </div>
)
